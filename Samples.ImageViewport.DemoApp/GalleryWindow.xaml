<Window x:Class="Samples.ImageViewport.DemoApp.GalleryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vp="clr-namespace:MUI.Controls.ImageViewport;assembly=MUI.Controls.ImageViewport"
        xmlns:vm="clr-namespace:Samples.ImageViewport.DemoApp.ViewModels"
        Title="Gallery" Width="1100" Height="720" Background="#181a1f">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <ScrollViewer>
        <WrapPanel x:Name="wrap" Margin="12" ItemWidth="520" ItemHeight="320">
            <Border Margin="8" BorderBrush="#333" BorderThickness="1" Background="#101215">
                <Grid>
                    <TextBlock Text="Template A" Foreground="#ccc" Margin="8" />
                    <vp:ImageViewport x:Name="vpA" Margin="0,28,0,0" Background="#101215"
                                      MinScale="{Binding Viewport.MinScale}"
                                      MaxScale="{Binding Viewport.MaxScale}"
                                      ScaleFactor="{Binding Viewport.ScaleFactor}"/>
                </Grid>
            </Border>
            <Border Margin="8" BorderBrush="#333" BorderThickness="1" Background="#101215">
                <Grid>
                    <TextBlock Text="Template B" Foreground="#ccc" Margin="8" />
                    <vp:ImageViewport x:Name="vpB" Margin="0,28,0,0" Background="#101215"
                                      MinScale="{Binding Viewport.MinScale}"
                                      MaxScale="{Binding Viewport.MaxScale}"
                                      ScaleFactor="{Binding Viewport.ScaleFactor}"/>
                </Grid>
            </Border>
        </WrapPanel>
    </ScrollViewer>
</Window>